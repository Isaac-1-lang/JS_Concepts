function input(sentence) {
//     let regExp =  /^[A-Za-z].*\.$/;
//     return regExp.test(sentence);
// }
// console.log(input("NIYOBYOSE ISAAC"));
// // Checking if a fraction is proper or No
// function isProper(numerator,denominator) {
//     if(numerator > denominator) {
//         return `${numerator} + "/" + ${denominator} It is improper fraction`; 
//     } else {
//         return `${numerator}/${denominator} It is proper fraction`;
//     }
// }
// console.log(isProper(34,2));

